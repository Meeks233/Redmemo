# Research notes
docs/redlib.md
docs/reddit.md

# Redlib reference clone
_redlib_ref/

# Build output
bin/

# Runtime config (contains secrets)
config.yaml
!config.example.yaml

# Internal docs
docs/structure.md
CLAUDE.md

# Claude Code turn-end marker (used by deploy.ps1 watch mode)
.cc-turn-done

# Local Claude Code settings (per-machine)
.claude/

# Windows accidental redirect artifact (e.g. `> /dev/null` parsed as `> nul`)
nul

.vscode

docs